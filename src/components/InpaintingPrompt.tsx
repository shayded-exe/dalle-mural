import { chakra } from '@chakra-ui/react';
import { observer } from 'mobx-react-lite';

const _InpaintingPrompt = ({}: {}) => {
  return <></>;
};

export const InpaintingPrompt = chakra(observer(_InpaintingPrompt));
